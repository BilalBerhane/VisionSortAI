"""Analyze and Backup — edge/IoT pipeline package.

SD card photo -> exiftool metadata -> perceptual-hash dedup ->
local blur/exposure checks -> AI vision agent classification ->
routing (delete / document record / photo backup).

See ../README.md for the full architecture and threshold rationale.
"""
